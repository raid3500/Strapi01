'use strict';

/**
 * mission-core-value router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::mission-core-value.mission-core-value');
