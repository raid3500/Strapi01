'use strict';

/**
 * mission-core-value service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mission-core-value.mission-core-value');
